meta {
  name: Register User 1
  type: http
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "name": "Race User 1",
    "email": "{{user1_email}}",
    "password": "{{password}}",
    "password_confirmation": "{{password}}"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setVar("token1", res.body.token);
    bru.setVar("user1_id", res.body.user?.id || "");
  }
}

assert {
  res.status: eq 201
}
