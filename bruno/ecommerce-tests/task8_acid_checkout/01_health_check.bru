meta {
  name: 1. Health Check
  type: http
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
